# Hardware compatibility matrix

> **Stub.** The full GPU/VRAM tier matrix with benchmark numbers (tokens/sec, TTS latency) lands in Phase 4 Slice 3. The short version is in the **Hardware requirements** section of the top-level [`README.md`](../README.md). The model recommendation logic itself lives in [`scripts/detect_hardware.sh`](../scripts/detect_hardware.sh) and is authoritative — the docs follow it.
