# Upgrade guide

> **Stub.** Upgrade procedures for moving between releases land in Phase 4 Slice 5, alongside `CHANGELOG.md`. Pre-v1.0.0 users tracking `main` can pull and `docker compose up -d --build` as usual; the setup wizard is idempotent and will not re-run once `setup_state.json` records `setup_completed: true`.
