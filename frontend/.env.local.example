# Dashboard auth token (single-user password)
DASHBOARD_TOKEN=changeme

# Orchestrator URL (server-side, Docker internal network)
# Client code hits same-origin /api/proxy/* which the Next.js server forwards here.
ORCHESTRATOR_URL=http://orchestrator:8888
