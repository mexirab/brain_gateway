# Brain Gateway orchestrator dependencies
# Pin major.minor to allow patch updates but prevent breaking changes
fastapi>=0.135,<1.0
uvicorn[standard]>=0.41,<1.0
httpx>=0.28,<1.0
python-dotenv>=1.2,<2.0
pydantic>=2.12,<3.0
python-multipart>=0.0.18,<1.0
pydantic-settings>=2.13,<3.0

# AI/ML
chromadb>=1.5,<2.0
sentence-transformers>=5.3,<6.0
einops>=0.8,<1.0
rapidfuzz>=3.14,<4.0

# Infrastructure
pyyaml>=6.0,<7.0
paramiko>=4.0,<5.0
apscheduler>=3.11,<4.0
croniter>=2.0,<3.0  # recurring-reminders rule expansion (settings page)

# Google APIs
google-auth>=2.49,<3.0
google-auth-oauthlib>=1.3,<2.0

# Monitoring
prometheus-client>=0.21,<1.0

# Encryption
cryptography>=44.0,<45.0

# Document processing
PyMuPDF>=1.25,<2.0
pytesseract>=0.3,<1.0
Pillow>=11.0,<12.0

# Integrations
ynab>=4.0,<5.0

# Budget import (CSV/Excel parsing + aggregation)
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
